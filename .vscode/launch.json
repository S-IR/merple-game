{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "preLaunchTask": "run-dev",
            "name": "Debug",
            "program": "${workspaceFolder}/build/debug.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}